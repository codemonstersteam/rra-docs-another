# Как контрибьютить

1. Форкни и заведи ветку `feat/<name>`.
2. `go test ./...` зелёный, `gofmt` чисто.
3. Открой PR с описанием «зачем».

Стиль кода — `gofmt` + `go vet`. Коммиты — Conventional Commits.
